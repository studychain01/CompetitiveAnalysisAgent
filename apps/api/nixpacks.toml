# Optional Nixpacks hints. On Railway prefer ``Dockerfile`` + ``railway.json`` in this directory.

[phases.install]
cmds = ["pip install --no-cache-dir ."]

[start]
cmd = "python -m uvicorn battlescope_api.main:app --host 0.0.0.0 --port $PORT"
